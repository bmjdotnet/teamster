# Logrotate config for Teamster's event journal.
# Delivered to /etc/logrotate.d/teamster by the installer (--wire mode).
__BASEDIR__/events.jsonl {
    weekly
    rotate 4
    compress
    copytruncate
    notifempty
    missingok
}
